(function (window) {
  window.__env = window.__env || {};

  // Environment variables
  window.__env.BACKEND_URL = "${REACT_APP_BACKEND_URL}";
  // You can add more environment variables here
})(this);